Custom group **{data.events[0].payload.name}** has been deleted by [{data.events[0].payload.username}]({environment.url}/iam/user-access/users/detail/{data.events[0].payload.username}?from=notifications&integration=drawer) and is not available anymore. [Open User Access]({environment.url}/iam/user-access/overview?from=notifications&integration=drawer)
