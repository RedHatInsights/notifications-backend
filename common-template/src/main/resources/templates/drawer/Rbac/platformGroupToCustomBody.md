{#include drawer/Common/commonDrawerNotification.md}
{#body}
**[{data.events[0].payload.username}]({environment.url}/iam/user-access/users/detail/{data.events[0].payload.username}?{query_params})** changed the Default access group. The group is now called **[{data.events[0].payload.name}]({environment.url}/iam/user-access/groups/detail/{data.events[0].payload.uuid}?{query_params})**.
{/body}
{/include}
