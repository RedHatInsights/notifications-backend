{#include drawer/Common/commonDrawerNotification.md}
{#body}
Platform default group **[{data.events[0].payload.name}]({environment.url}/iam/user-access/groups/detail/{data.events[0].payload.uuid}?{query_params})** has been modified by **[{data.events[0].payload.username}]({environment.url}/iam/user-access/users/detail/{data.events[0].payload.username}?{query_params})** and will be managed by the Organization Administrator. It will receive no further updates from Red Hat Hybrid Cloud Console.
{/body}
{/include}
