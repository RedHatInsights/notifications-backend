Red Hat added a role **[{data.events[0].payload.role.name}]({environment.url}/iam/user-access/roles/detail/{data.events[0].payload.role.uuid}?from=notifications&integration=drawer)** to platform default access group.
