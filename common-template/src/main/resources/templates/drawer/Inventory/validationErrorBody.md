{#include drawer/Common/commonDrawerNotification.md}
{#body}
Data in a payload from insights-client was unable to be processed in the inventory due to corrupted data, incorrect values, or another issue. If no hosts were created by this change, the error will not appear in the service. [Open Inventory]({environment.url}/insights/inventory?{query_params})
{/body}
{/include}
