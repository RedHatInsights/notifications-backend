{#include drawer/Common/commonDrawerNotification.md}
{#body}
The **[{data.events[0].payload.display_name}]({environment.url}/insights/tasks/executed/{data.context.executed_task_id}?{query_params})** job from the **{data.context.task_name}** task failed with the **{data.events[0].payload.status}** status.
{/body}
{/include}
