Your system, **{data.events[0].payload.host_name}**, assigned to policy **{data.events[0].payload.policy_name}**, has been marked as non-compliant because its compliance score {data.events[0].payload.compliance_score}% dropped below the configured compliance threshold of {data.events[0].payload.policy_threshold}% for this policy.
