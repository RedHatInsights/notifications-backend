Your system, **[{data.events[0].payload.host_name}]({environment.url}/insights/compliance/systems/{data.events[0].payload.host_id}?from=notifications&integration=drawer)**, assigned to policy **[{data.events[0].payload.policy_name}]({environment.url}/insights/compliance/scappolicies/{data.events[0].payload.policy_id}?from=notifications&integration=drawer)**, has been marked as non-compliant because its compliance score {data.events[0].payload.compliance_score}% dropped below the configured compliance threshold of {data.events[0].payload.policy_threshold}% for this policy.
