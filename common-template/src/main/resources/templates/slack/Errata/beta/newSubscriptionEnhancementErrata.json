{#include slack/Common/beta/commonSlackBlocks.json}
    {#content-intro}There {#if data.events.size() == 1}is 1 enhancement{#else}are {data.events.size()} enhancements{/if} affecting your subscriptions.{#if data.events.size() > 3}\nFirst 3 are:{/if}{/content-intro}
    {#content-body}
        {#each data.events}
        {#if it_index < 3}
        {
            "type": "section",
            "text": {
                "type": "mrkdwn",
                "text": "\u2699\uFE0F <{data.context.base_url}{it.payload.id}?from=notifications&integration=slack|*{it.payload.id}*>\n{it.payload.synopsis}"
            }
        },
        {/if}
        {/each}
        {
            "type": "divider"
        },
        {
            "type": "section",
            "text": {
                "type": "mrkdwn",
                "text": "Explore {#if data.events.size() != 1}these{#else}this{/if} and others in the <https://access.redhat.com/errata-search/?from=notifications&integration=slack|errata search>."
            }
        }
    {/content-body}
    {#explore-url}https://access.redhat.com/errata-search/?from=notifications&integration=slack{/explore-url}
    {#explore-location}errata search{/explore-location}
{/include}
