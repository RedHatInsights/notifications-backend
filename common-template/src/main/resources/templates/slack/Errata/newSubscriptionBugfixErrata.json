{#include slack/Common/commonSlackBlocks.json}
    {#content-intro}There {#if data.events.size() == 1}is 1 bug fix{#else}are {data.events.size()} bug fixes{/if} affecting your subscriptions.{#if data.events.size() > 3}\nFirst 3 are:{/if}{/content-intro}
    {#content-body}
        {#each data.events}
        {#if it_index < 3}
        {
            "type": "section",
            "text": {
                "type": "mrkdwn",
                "text": "\uD83D\uDD27 <{data.context.base_url}{it.payload.id}?from=notifications&integration=slack|*{it.payload.id}*>\n{it.payload.synopsis}"
            }
        },
        {/if}
        {/each}
        {
            "type": "divider"
        },
    {/content-body}
    {#explore-url}https://access.redhat.com/errata-search/?from=notifications&integration=slack{/explore-url}
    {#explore-location}errata search{/explore-location}
{/include}
