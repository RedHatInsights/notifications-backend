notifications.connector.http.connect-timeout-ms=200
notifications.connector.http.socket-timeout-ms=200
quarkus.rest-client.bop.read-timeout=500
quarkus.rest-client.recipients-resolver.read-timeout=500

notifications.connector.redelivery.delay=5
notifications.connector.redelivery.max-attempts=0

notifications.connector.max-recipients-per-email=4

# CloudWatch logging configuration for tests (disabled)
quarkus.log.cloudwatch.enabled=false

# Connector configuration for tests
notifications.connector.kafka.incoming.topic=test-incoming-topic
notifications.connector.kafka.incoming.group-id=test-group-id
notifications.connector.kafka.outgoing.topic=test-outgoing-topic

# Test configuration for Quarkus connectors

# In-memory messaging configuration for tests
mp.messaging.incoming.incoming-notifications.connector=smallrye-in-memory
mp.messaging.outgoing.outgoing-notifications.connector=smallrye-in-memory
mp.messaging.outgoing.processed-notifications.connector=smallrye-in-memory

# REST client configuration for tests
quarkus.rest-client.connection-pool-size=5
